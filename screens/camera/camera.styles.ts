import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  camera: {
    flex: 1,
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
  captureButton: {
    height: 64,
    width: 64,
    borderRadius: 32,
    marginBottom: 12,
    borderColor: '#888',
    borderWidth: 8,
    backgroundColor: '#fff',
  },
});
